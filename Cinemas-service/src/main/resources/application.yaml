server:
  port: 8080
  servlet:
    context-path: /cinema

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/cinema_service"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true