server:
  port: 8080
  servlet:
    context-path: /movie
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/movie_service"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true